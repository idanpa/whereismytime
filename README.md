# Where is my time?
Find out where is your time. Simple time management CLI.

## Installation
```bash
pip install wmt
```
## Usage
```bash
$ wmt start -n "project a, task b, initial work"
$ wmt end
```
